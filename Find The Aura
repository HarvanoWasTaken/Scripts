for i = 1, #workspace.nintenblock12:GetChildren(), 1 do 

if workspace.nintenblock12.HumanoidRootPart:GetChildren()[i].ClassName ~= "Part" then

workspace.nintenblock12[i].CanCollide = false

end 
end

workspace.nintenblock12.HumanoidRootPart.CanCollide = false

for i = 1, #workspace.Morphs:GetChildren(), 1 do 

if workspace.Morphs:GetChildren()[i].ClassName ~= "Script" then
local AuraName = workspace.Morphs:GetChildren()[i].Name

for v = 1, 60, 1 do
wait(0)

workspace.nintenblock12.HumanoidRootPart.CFrame = workspace.Morphs:GetChildren()[i].CFrame

end

end 
end
